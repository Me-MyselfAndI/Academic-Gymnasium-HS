#include <iostream>
using namespace std;

int main() {
    string a;
    cin >> a;
    int n = 0;
    for(int i = 0; i < a.size(); ++i){
        if(a[i] == 'w') ++n;
    }
    cout << n;
    return 0;
}
